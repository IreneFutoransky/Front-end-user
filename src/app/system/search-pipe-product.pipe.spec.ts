import { SearchPipeProductPipe } from './search-pipe-product.pipe';

describe('SearchPipeProductPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPipeProductPipe();
    expect(pipe).toBeTruthy();
  });
});
